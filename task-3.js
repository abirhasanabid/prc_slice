/**
 * Task: Use the slice method to create a new array that contains the last two elements of the colors array using negative indices.
 */

let colors = ["red", "green", "blue", "yellow", "purple"];

// console.log(colors.slice(colors.length-2,colors.length-0))
// -1,-2//
console.log(colors.slice(-2));
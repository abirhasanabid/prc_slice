/**
 * Task: Use the slice method in a way that returns an empty array. Experiment with different start and end indices to achieve this.
 */

let fruits = ["apple", "banana", "cherry", "date", "elderberry"];

console.log(fruits.slice(2,2)); //When the start and end indices are the same, slice returns an empty array.

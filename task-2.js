/**
 * Task: Use the slice method to extract the word "JavaScript" from the sentence string.
 */

const sentence = "JavaScript is awesome!";

extractWord = sentence.slice(0,10);
console.log(extractWord);
